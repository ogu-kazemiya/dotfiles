# Dotfiles

Mac用

```bash
git clone https://github.com/ogu-kazemiya/dotfiles.git ~/dotfiles
cd ~/dotfiles
./setup.sh
```


## 手動で実行するもの

### Google日本語入力
入力ソース設定は手動

```bash
# 設定のコピー
cp -r "$HOME/dotfiles/google-ime/config1.db" "$HOME/Library/Application Support/Google/JapaneseInput/config1.db"
```
